{
  "name": "reservat",
  "private": true,
  "scripts": {
    "start": "concurrently -k -n backend,frontend -c blue,magenta \"npm run start:backend\" \"npm run start:frontend\"",
    "start:backend": "cd reservation-backend && mvnw.cmd spring-boot:run",
    "start:frontend": "cd reservation-frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
